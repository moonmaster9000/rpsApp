{
  "name": "web",
  "version": "0.0.0",
  "dependencies": {
    "rps": "file:../rps",
    "rpsPresentationI18n": "file:../rpsPresentationI18n",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "babelify": "latest",
    "babel-preset-es2015": "latest",
    "babel-preset-react": "latest",
    "webSpecs": "file:../webSpecs"
  },
  "scripts": {
    "compile": "browserify main.js -o bundled/compiled_main.js -t [ babelify --presets [ es2015 react ] ]",
    "compile-test": "browserify spec/testSpec.js -o bundled/compiled_test.js -t [ babelify --presets [ es2015 react ] ]"
  }
}